#include "main.h"

/**
 * _abs - computes the absolute value of a number
 * @n: input
 * Return: (0)
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = (n * -1);
	}
	else
	{
		n = (n * 1);
	}
	return (0);
}

